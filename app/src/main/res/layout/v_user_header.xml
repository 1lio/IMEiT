<?xml version="1.0" encoding="utf-8"?>
<merge xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:gravity="center_vertical">

    <ru.vyaacheslav.suhov.imeit.view.view.AvatarView
        android:id="@+id/avatar"
        android:layout_width="@dimen/size_avatar_header"
        android:layout_height="@dimen/size_avatar_header"
        android:layout_gravity="start|center"
        android:layout_margin="@dimen/activity_vertical_margin"
        android:src="@drawable/group"
        app:strokeColor="@color/colorAccent"
        app:strokeWidth="@dimen/width_stroke"
        tools:srcCompat="@tools:sample/avatars" />

    <LinearLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:layout_marginStart="@dimen/activity_vertical_padding"
        android:gravity="center"
        android:orientation="vertical">

        <TextView
            android:id="@+id/u_name"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:textSize="18sp"
            android:textStyle="bold"
            tools:text="Viacheslav Sukhov" />

        <TextView
            android:id="@+id/u_group"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            tools:text="ISiT-31" />
    </LinearLayout>

    <ImageView
        android:id="@+id/exit"
        android:layout_width="@dimen/size_btn_exit_header"
        android:layout_height="@dimen/size_btn_exit_header"
        android:layout_gravity="end|center"
        android:layout_margin="@dimen/activity_vertical_margin"
        android:contentDescription="@string/exit_for_profile"
        android:tint="@color/gray"
        app:srcCompat="@drawable/ic_exit" />

</merge>